{% include '@real/includes/header.html.twig' %}

<div class="usercContent">

    {% if page.banner %}
      <div class="container">
        <div class="text-center">
          <div class="row">
            <div class="col-md-12">{{ page.banner }}</div>
          </div>
        </div>
      </div>
    {% endif %}


{% if page.content %}
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          {{ page.content }}
        </div>
      </div>
    </div>
{% endif %}

</div>

{% include '@real/includes/footer.html.twig' %}