{% include '@real/includes/header.html.twig' %}

<header class="main-header">
  {% if page.banner %}
    <div class="container">
      <div class="banner">
        <div class="row">
          <div class="col-md-12">{{ page.banner }}</div>
        </div>
      </div>
    </div>
  {% endif %}
</div>

  {% if page.content %}
  <div class="container">
    <div class="row">
      <div class="col-md-12 contenido">
        {{ page.content }}
      </div>
    </div>
  </div>
  {% endif %}

{% include '@real/includes/footer.html.twig' %}
